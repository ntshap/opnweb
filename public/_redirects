# Netlify redirects file
# Handle client-side routing for Next.js

# Redirect dashboard paths to login if not authenticated
/dashboard/*    /login    302    Role=anonymous

# Redirect all paths to index.html with 200 status code
/*    /index.html   200
